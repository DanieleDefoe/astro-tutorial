---
import Main from "@/layouts/main.astro";

const pageTitle = "About Me";

const identity = {
  firstName: "Daniel",
  country: "Russia",
  occupation: "Developer",
  hobbies: ["photography", "birdwatching", "baseball"],
};

const skills = ["HTML", "CSS", "JavaScript", "React", "Astro", "Writing Docs"];

const happy = true;
const finished = true;
const goal = 3;
---

<Main title={pageTitle}>
  <h2>My name is {identity.firstName}</h2>
  <p>I live in {identity.country}</p>
  <p>I am a {identity.occupation}</p>
  {happy && <p>I am happy</p>}
  {finished && <p>I have finished</p>}
  {goal > 0 && <p>I have a goal</p>}
  <ul>
    {
      identity.hobbies.map((hobby) => (
        <li class="text-2xl text-green-500">{hobby}</li>
      ))
    }
  </ul>
  <ul>
    {
      skills.map((skill) => (
        <li class="text-4xl font-bold text-blue-500 uppercase">{skill}</li>
      ))
    }
  </ul>
</Main>
